#include <iostream>
#include <string>
#include <bitset>

using namespace std;

int main()
{
	for(int n, r; r<247 ; n++)
	{
		if (n%3 == 0)
		{
			string binary = bitset<>(n);
		}
	}
	
	return 0;
}
